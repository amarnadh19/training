pipeline {
    agent any
    stages {
        stage ("Git Clone"){
            steps {
               git credentialsId: 'traininggit', url: 'https://github.com/amarnadh19/training.git'

                sh "ls -lat"
            }
        }
    }
}